# portfolio-value

`pip install -r requirements.txt`


Update `config.json`, put your `YOUR_alphavantage_api_key`


to get the data 
`python parser-controller.py` 


to run the display
`python ./webapp/index.py`